//  CococoJinja — the canonical Jinja-subset expression engine.
//
//  PLACEHOLDER. Replaced by the engine extracted verbatim from WorkflowKit's
//  `Sources/WorkflowKit/Sources/WorkflowKit/Expression/` (Lexer / Parser /
//  ExpressionAST / Interpreter / Filters / FilterRegistry / ExpressionTemplate /
//  ExpressionError / ExpressionLimits / ExpressionValidator / Printer / Token)
//  plus `WorkflowValue` (renamed `JinjaValue`) and its semantics/JSON files.
//
//  Standalone, no external deps, Linux-capable. Depended on by WorkflowKit,
//  SurfaceKit, cococo-app, and the @cococo/jinja TS peer (via shared fixtures).

public enum CococoJinja {}
