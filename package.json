{
  "name": "@cococo/jinja",
  "version": "0.0.0",
  "description": "TS peer of the canonical Jinja-subset expression engine. Same behaviour as Swift CococoJinja, proven by shared fixtures.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./types/index.d.ts",
      "default": "./ts/src/index.ts"
    }
  },
  "files": ["ts/src", "types", "fixtures"],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:types": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
